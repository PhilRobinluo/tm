#!/bin/zsh
# ================================================================
# tm 插件示例
# ================================================================
# 将 .sh 文件放到 ~/.tm/plugins/ 目录即可自动加载
#
# 插件可以：
# 1. 定义新的 do_xxx() 函数，在工具箱菜单中调用
# 2. 覆盖现有函数来修改行为
# 3. 添加新的快捷命令
#
# 规范：
# - 函数名用 do_xxx() 格式
# - 用 teach() 展示对应的原始命令
# - 用 pause() 让用户看到结果
# ================================================================

# 示例：Docker 容器一览
# do_docker_ps() {
#     clear
#     echo ""
#     echo "  ${BD}🐳 Docker 容器状态${NC}"
#     line
#     echo ""
#     docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}" 2>/dev/null
#     teach "docker ps" "列出所有运行中的容器"
#     pause
# }
